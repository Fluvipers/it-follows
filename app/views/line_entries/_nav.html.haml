%nav.navbar.navbar-default
  .container-fluid
    .navbar-header
      - @lines.each do |line|
        %p.navbar-text.navbar-right
          %a.navbar-brand
            = link_to line.name.humanize, line_entries_path(line_entries: line.slug_name)
= yield
